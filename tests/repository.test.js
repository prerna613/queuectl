const JobRepository = require('../src/repositories/JobRepository');

describe('Repository', () => {

    test('Repository object exists', () => {

        expect(JobRepository).toBeDefined();

    });

});